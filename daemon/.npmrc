# The @xterm/addon-* packages declare peerDependencies on @xterm/xterm@^5
# but work correctly with ^6. This flag suppresses the resulting ERESOLVE
# error so plain `npm install` works without extra flags.
legacy-peer-deps=true
